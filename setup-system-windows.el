;; Use windows key as super (map capslock to apps key)
;; In AutoHotKey with #IfWinActive, emacs { Capslock::AppsKey }
(setq w32-apps-modifier 'super)

;; Use a nice font
(set-frame-font "DejaVu Sans Mono-10")
